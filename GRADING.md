# 批改系統使用說明（grade.py）

學生把作品上傳到 Padlet → AI 依 rubric 逐項判斷並寫評語**草稿** → 老師在審核頁核准、修改或補一句 → 按「發布」才會留言到學生的貼文下。
方案背景見 [docs/方案說明-美術課AI評語小幫手.md](docs/方案說明-美術課AI評語小幫手.md)。

以下指令都在 repo 資料夾（`ollama-vision-setup`）的 PowerShell 執行。

## 第一次使用
1. 裝好套件（已經跑過 `scripts\40-python-env.ps1` 的話，再跑一次就會補裝新套件）
2. 複製 `.env.example` 成 `.env`，填入 `PADLET_API_KEY`
   - 到 https://padlet.com/dashboard/settings/developers 產生（需要付費帳號）
   - 你必須是作業版的**管理員**（自己建立的，或被加為管理員）
   - 作業版要在設定裡**開啟留言**
3. 確認 key 能用、看看系統怎麼把貼文分給學生：
   ```powershell
   .venv\Scripts\python.exe -X utf8 grade.py check <Padlet網址>
   ```

## 寫 rubric
rubric 是 `.toml` 檔：TOML 是一種純文字設定檔，用記事本就能開，一行一個設定（`名稱 = "內容"`），`#` 後面是註解。
複製 `rubrics\範例-攝影三作業.toml`，改成這份作業的內容（也可以在作業牆管理的「評分規準」分頁線上改）。重點：
- **一個區段＝一份作業**：`[[assignments]]` 是一份作業，`name` 對應作業牆的區段名稱（區段「① 微距攝影」對上作業「微距攝影」），底下的 `[[assignments.criteria]]` 只用在這份作業；最上層的 `[[criteria]]` 是每份作業都要看的共同檢查點
- **檢查點**寫「看得見、能指出位置」的描述，每份作業 3～5 項（含共同的）
- `work` 寫「什麼算這份作業的作品」，AI 判斷不是作品時會改成請學生重傳（攝影作業拍到人是對的，所以要寫清楚）
- AI 判斷不可靠或太主觀的項目（明暗、透視、人物神情）加 `ai = false`，審核頁會留給你選等級
- `levels` 是等級（由好到壞），`points` 是對應分數；分數只出現在審核頁和匯出的 CSV，**不會貼給學生**
- `[feedback]` 放語氣、長度、不要出現的話、`praise_angles`（肯定學生時可以挑的角度），以及 2～3 則你自己寫過的評語範本
- 沒有 `[[assignments]]` 的 rubric 是「整面牆一份作業」：同一位學生各區段的照片合起來評一次（`--unit student`）
- 用記事本編輯，存成 UTF-8；文字要加雙引號，中文的鍵名也要加雙引號（例如 `"符合" = "…"`）

## 每次批改
```powershell
.venv\Scripts\python.exe -X utf8 grade.py run <Padlet網址> --rubric rubrics\<你的rubric>.toml
```
rubric 有 `[[assignments]]` 時，每位學生每個區段是一份作品（一張審核卡、一則評語，留在該區段那一篇）；要強制切換加 `--unit section` 或 `--unit student`。
會依序：下載照片 → 每位學生寫草稿（約 10～15 秒／人）→ 自動打開審核頁 `http://127.0.0.1:8765`。
中途關掉沒關係，再跑一次會接著做；已核准、已發布、已略過的學生不會被改動。學生後來補傳照片，重跑會替他重寫草稿。

只想打開審核頁（接著上次進度）：
```powershell
.venv\Scripts\python.exe -X utf8 grade.py review
```

## 審核頁怎麼用
每位學生一張卡片：左邊是作品照片（點一下放大），右邊是 AI 的判斷與評語草稿。
- **黃色標籤**是要你特別看的地方：照片模糊、AI 無法判斷、請老師判斷的項目、評語疑似直接給答案等
- **等級**可以直接改；改完按「重寫評語」，評語會跟著調整
- **評語方向**：全部達到最高等級時是「鼓勵」（肯定＋延伸挑戰的提問），否則是「修正建議」（先肯定做到的，再用提問引導學生自己發現要改哪裡）。可以手動切換
- **重寫評語**可以給 AI 一句提示（例如「多肯定背景的筆觸」）；**重新看圖**會重新判斷所有項目
- 評語可以直接改；「老師補一句」會接在評語後面，適合寫透視、比例、明暗的專業建議
- **核准**（或在評語框按 Ctrl+Enter）→ 卡片移到「已核准」。這時**還沒發出去**
- 全部審完，按右上角「**發布已核准**」，輸入「發布」確認後，才會留言到每位學生的第一篇貼文下
- 「全班摘要」整理各檢查點的人數分布、共同亮點、常見問題與教學建議（只給老師看）
- 「匯出成績 CSV」可以用 Excel 開，含各項等級、分數與評語

⚠️ 留言發出後，系統**不能撤回**，要修改請到 Padlet 上手動處理。

## 讓美術老師從自己的電腦審核（校內網路）
審核頁預設只有這台電腦能開。要讓校內其他電腦連進來：
1. 設密碼（在自己的終端機輸入，畫面不顯示；`.env` 只存雜湊）：
   ```powershell
   .venv\Scripts\python.exe -X utf8 grade.py set-password
   ```
2. `.env` 設 `GRADER_HOST=0.0.0.0`（沒設密碼時伺服器會拒絕啟動）
3. 以**系統管理員身分**開 PowerShell，允許同網段連到 8765（只開校內，不對外）：
   ```powershell
   New-NetFirewallRule -DisplayName "評語審核頁 8765" -Direction Inbound -Protocol TCP -LocalPort 8765 -RemoteAddress LocalSubnet -Action Allow -Profile Any
   ```
4. 執行 `grade.py review`，畫面會列出校內網址（例如 `http://172.16.24.28:8765/`），把網址和密碼給美術老師

注意：
- 同一台電腦連續打錯 5 次密碼會鎖 10 分鐘；關掉瀏覽器就會登出
- 學生也在同一個校內網路，**密碼不要太簡單**：拿到密碼的人可以用你的 Padlet 帳號發評語
- 這台電腦用 Wi-Fi 時 IP 可能會變，網址以 `grade.py review` 當下顯示的為準
- 審核頁必須在這台電腦上執行（需要本機的 AI 模型和 Padlet key），**不能放到 GitHub**；`data/` 有學生姓名與作品，也不能上傳
- 不用時把 `GRADER_HOST` 改回 `127.0.0.1`，或關掉審核頁

## 作業牆管理（建牆、抓作業，不用開指令）
審核頁右上角「作業牆管理」（`/portal`），登入密碼和審核頁同一組。四個分頁：
- **建作業牆**：選範本，填班級、作業牆名稱，設定**區段數量與名稱（一個區段＝一份作業）**、補充說明（選填，例如截止日）→「建立作業牆」。每個區段旁邊會顯示有沒有對應到評分規準的作業。Padlet 的 AI 建牆約 30～90 秒，建好會讀回檢查區段和標題，不一樣時會出現黃色提醒
- **作業牆清單**：每面牆有三項**要到 Padlet 手動做**的待辦（開留言、關貼文審核、確認版型與區段順序；Padlet API 做不到）。三項都打勾後才會出現給學生的網址、QR code（可下載）。每面牆可以選評分規準、查繳交人數，按「**抓作業＋寫草稿**」會在背景跑，完成後點「進入審核頁」
- **範本**：建牆時的預設值：區段（一行一個）、給學生看的說明、評分規準。Padlet 牆建好後不能用程式加區段，要到 Padlet 手動加
- **評分規準**：選 `rubrics\` 裡的 .toml 線上編輯，或上傳 .toml。存檔前會檢查格式，有錯不會蓋掉原檔
- **使用說明**：流程、區段與作業的關係、.toml 怎麼寫、常見問題；「建作業牆」分頁上方也有五個步驟的引導

注意：
- 新牆都建在 `.env` 那組 Padlet key 的帳號（pad02_98）底下；老師要改牆的設定，要登入這個帳號，或請管理者把老師加為這面牆的管理員
- 同一時間只能有一面牆在「抓作業＋寫草稿」（顯示卡只有一張）；背景處理中的那面牆，審核頁暫時不能修改
- 建牆紀錄在 `data\boards.json`、範本在 `data\templates.json`（不進 git）。「從清單移除」只刪紀錄，Padlet 上的牆不會被刪

## 學生怎麼上傳，系統怎麼分辨是誰
Padlet 每篇貼文只有一個附件，學生傳 2～3 張照片就是 2～3 篇貼文。系統會把同一位學生的貼文合併：
- 學生**有登入** Padlet → 依帳號分（最準）
- 有登入和**訪客**（沒帳號但有填名字）混在一起 → 有帳號的依帳號、訪客依名字。同一位訪客每次要填一樣的名字
- 學生**匿名**上傳 → 依貼文**標題**分，請學生在標題寫「座號 姓名」，每篇都一樣
- 也可以用 `--group-by section`（每位學生一欄）或 `--group-by post`（一篇就是一份）
- 老師自己發的貼文（作業說明）會自動排除

先用 `grade.py check <網址>` 看分組對不對，再正式跑。

## 用本機資料夾試跑（不會發到 Padlet）
每位學生一個子資料夾，裡面放作品照片（JPG／PNG／HEIC 都可以）：
```
D:\試跑作品\
├── 01王小明\  1.jpg  2.jpg
└── 02陳小華\  1.jpg
```
```powershell
.venv\Scripts\python.exe -X utf8 grade.py run D:\試跑作品 --rubric rubrics\範例-攝影三作業.toml --unit student
```
適合在正式使用前，用去年的作品和老師自己的判斷比對 AI 準不準（方案說明第三節第 5 點）。

## 資料放在哪裡
- `data\<作業版id>\state.json`：每位學生的狀態、AI 判斷、評語、審核紀錄
- `data\<作業版id>\images\`：下載的作品照片
- `data\<作業版id>\成績-*.csv`：匯出的成績
- `data\` 含學生姓名與作品，**已列入 .gitignore，不會進 git**。學期結束請自行刪除

## 其他指令
```powershell
.venv\Scripts\python.exe -X utf8 grade.py list              # 列出處理過的作業版
.venv\Scripts\python.exe -X utf8 grade.py fetch <網址> --rubric ...   # 只下載，不寫草稿
.venv\Scripts\python.exe -X utf8 grade.py draft [--redo]    # 替還沒有草稿的學生寫草稿；--redo 連未核准的也重寫
.venv\Scripts\python.exe -X utf8 grade.py export            # 匯出成績 CSV
.venv\Scripts\python.exe -X utf8 -m unittest tests\test_grader.py -v   # 離線測試
```

## 評語是怎麼產生的
1. **看圖判斷**：同一位學生的照片（最多 4 張，長邊 1536px）一次送給模型，逐項寫出「在畫面哪裡看到什麼」，再依此選等級；另外寫畫面描述、優點位置、照片問題
2. **寫評語**：依等級決定「鼓勵」或「修正建議」。修正建議挑最弱的 1～2 項，每項寫一個提問，不直接給答案
3. **自我檢查**：另外請模型檢查第一句肯定有沒有把「還沒做到的地方」說成優點（小模型常犯），有就只重寫那一句；結尾的鼓勵也同樣檢查
4. **簡體字**：逐字檢查，只轉換真的是簡體的字，並標記給老師看
