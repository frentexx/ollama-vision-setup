# 驗收標準

`scripts\50-verify.ps1` 會自動檢查下表的前五層，結果寫到 `reports\acceptance-<電腦名稱>-<日期>.md`。
第六層由人工確認，勾在報告最下方。**有任何 FAIL 就不算通過。**

| 層 | 檢查項目 | 通過標準 | 怎麼量 |
|---|---|---|---|
| 1 服務 | API 回應 | `/api/version` 有回應 | HTTP 請求 |
| | 只給本機使用 | 監聽位址只有 `127.0.0.1` 或 `::1` | netstat |
| | 模型已下載 | 有 `qwen3-vl:8b-instruct`，能力含 vision | `/api/tags`、`/api/show` |
| | 開機自動啟動 | 啟動資料夾有 Ollama 捷徑（缺少只算 WARN） | 檔案檢查 |
| 2 GPU | 100% 使用 GPU | 顯存占用 ÷ 模型大小 = 100% | `/api/ps`（等同 `ollama ps` 的 PROCESSOR 欄） |
| | 模型常駐 | 到期時間在一年以後（KEEP_ALIVE=-1） | `/api/ps` |
| 3 功能 | 合法 JSON | 5 個欄位齊全、型別正確 | 3 張測試圖各跑一次 |
| | 繁體中文 | 沒有簡體字（opencc 比對，排除 才／吃／核） | 同上 |
| | 正確描述 | OCR 圖讀出「屏北」「美術」；色塊圖和斜拍圖都提到「紅」「藍」 | 關鍵字比對；只命中一部分算 WARN |
| 4 效能 | 單張速度 | 長邊 1024px，3 次取中位數 **≤15 秒**，並記錄 tok/s | 計時 |
| 5 壓力 | 一節課的量 | 20 位學生、共 50 張（長邊 1536px），**≤10 分鐘** | 依序處理 |
| | JSON 全部合格 | 20/20 | |
| | 溫度 | GPU 最高 **≤85°C** | nvidia-smi 每 2 秒取樣 |
| 6 人工 | 不對外 | 從**另一台電腦**用瀏覽器開 `http://<目標機IP>:11434` 連不上 | 人工 |
| | 沒有幻覺 | 報告裡的評語樣本沒有描述畫面上不存在的東西 | 人工看 |
| | 重開機自動恢復 | 重開機後不用手動操作，`ollama ps` 就能看到服務 | 人工 |

## 測試圖
由 `verify.py` 自動產生，存放在 `tests\images\generated\`（不進 git）：
- `ocr.jpg`：白紙上寫「屏北高中 美術作業／第三週 色彩練習」
- `color.jpg`：左上紅圓、右下藍方、中央黃三角，下方寫「我的構圖」
- `photo.jpg`：把 color 圖做透視變形、偏黃、壓暗，模擬學生斜拍的照片

壓力測試會把這三張圖隨機裁切、旋轉、調亮度，湊成 50 張，避免內容完全相同被快取而測不準。

> 這些是**安裝驗收**用的合成圖，只能證明模型正常運作，不能代表美術評語的品質。
> 評語品質要等美術老師提供規準後，用 5 份真實作品另外評估（下一階段）。

## 簽核
驗收人在報告最下方勾選三項人工確認並簽名，再把報告 commit 回 repo。
